import { AngularFireDatabaseModule } from './angular-fire-database-module';

describe('AngularFireDatabaseModule', () => {
  it('should create an instance', () => {
    expect(new AngularFireDatabaseModule()).toBeTruthy();
  });
});
