import { AngularFireModule } from './angular-fire-module';

describe('AngularFireModule', () => {
  it('should create an instance', () => {
    expect(new AngularFireModule()).toBeTruthy();
  });
});
